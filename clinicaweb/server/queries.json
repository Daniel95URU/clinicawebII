{
    "login": "SELECT * FROM \"user\" WHERE username = $1 AND password = $2",
    "register": "INSER INTO \"user\" (username, password) VALUES ($1, $2)",

    "permission": "SELECT u.username, p.profile_name FROM users u INNER JOIN profiles p ON u.profile_id = p.profile_id;"
  }